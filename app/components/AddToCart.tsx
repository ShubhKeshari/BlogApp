'use client' // It will be render on client side
import React from 'react'
const AddToCart = () => {
  return (
    <button onClick={()=>console.log("click")}>Add To Cart</button>
  )
}

export default AddToCart